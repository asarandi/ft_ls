** add arrow -> and location of symbolic link to -l long output
** unify all width/length count functions into one
       (all = blocks, links, owner, group, size)

** add major/minor support for char devices

** implement command line options/arguments
** test/debug, implement error handling
